# sorted(iterable, key=None, reverse=False)
# key=len (By length the array will be sorted)
# reverse=True

fruits = ["banana", "apple", "potatato"]

print(fruits)
print(sorted(fruits, key=len, reverse=True))